{
  "name": "multi-agent-chat",
  "version": "2.0.0",
  "provides": ["service"],
  "config_schema": {
    "type": "object",
    "properties": {
      "default_manager_type": {
        "type": "string",
        "enum": ["round_robin", "supervisor", "dynamic", "sleeptime"],
        "default": "round_robin",
        "description": "Default Letta Group manager type for coordinating agent turns"
      }
    }
  },
  "advertise": true,
  "description": "Multi-agent conversation management using Letta Groups"
}
